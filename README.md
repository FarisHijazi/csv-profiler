# CLI+Streamlit simple CSV profiler example project

This is a simple example for teaching python

```sh
uvx git+https://github.com/FarisHijazi/csv-profiler.git profile data/sample.csv --out-dir outputs --format both
```
